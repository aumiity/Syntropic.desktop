---
name: project_receipt_sections
description: ใบเสร็จ thermal slip — per-section style model (show/bold/align), ขนาดอักษร global, threading พนักงานขาย
metadata:
  type: project
---

**Receipt per-section style — DONE 2026-06-08.** ทำใบเสร็จ (thermal slip) ให้ปรับสไตล์ราย "กลุ่ม/บรรทัด" ได้แบบเดียวกับ [[project_drug_label]] แต่ตัด offset X/Y ออก เหลือ **show + bold + align (ซ้าย/กลาง/ขวา/กระจาย)**.

**SSOT = `src/lib/receipt/sections.ts`** (`RC_SECTIONS`, `RcAlign`, `alignCss`). 10 กลุ่มเรียงตามใบจริง: shop / shop_contact / tax_id / title / bill_info / **items** / summary / payment / footer / salesperson. แต่ละกลุ่ม controllable มีคอลัมน์ `show_<key>` + `bold_<key>` + `align_<key>` บน `receipt_settings`. `items` = รายการขาย **ไม่มี control** (fixed 2-col, render verbatim, โชว์เป็น row อ่านอย่างเดียวในตั้งค่า).

**กฏสำคัญ:**
- **ขนาดอักษร = GLOBAL** (`receipt_settings.font_size` ตัวเดียวทั้งใบ — เจ้าของขอ "ปรับที่เดียวทั้งหน้า"). ไม่มี per-section size. เอาความต่างขนาดเดิม (shop name base+3, total base+3 ฯลฯ) ออกหมด.
- **align ของกลุ่ม "pair"** (bill_info/summary/payment): `justify` = กระจาย 2 คอลัมน์ (ป้ายซ้าย-ค่าขวา แบบเดิม); `left/center/right` = แพ็ค "ป้าย ค่า" ติดกันแล้วจัดทั้งบรรทัด. ดู `pairBlock()` ใน `buildSlipHtml.ts`.
- **header_note ถูกลบทิ้ง** (แอป + DB `ALTER TABLE receipt_settings DROP COLUMN header_note`). ใบเสร็จเหลือ free-text แค่ `footer_note`. ⚠️ ความหมายเปลี่ยน: tax_id + branch เดิมโชว์เฉพาะ abbrevTax → ตอนนี้โชว์เสมอถ้า shop มีค่า + show_* เปิด.
- **ความสูงกระดาษ + จำนวนสำเนา = ตัดออกจาก UI (2026-06-08)** — ใบเสร็จ = ม้วนต่อเนื่อง → height **auto เสมอ**, copies **= 1 เสมอ** (ไม่เปิดให้ปรับ). บังคับที่ source: `print.ts` `SLIP_HEIGHT='auto'`/`SLIP_COPIES=1` (ครอบคลุม POS + reprint), หน้าตั้งค่าก็ hardcode auto/1, และ load-merge ใน ReceiptSettingsTab skip `paper_height_mm`/`copies` (pin = 0/1, normalize DB ตอนบันทึก). คอลัมน์ `paper_height_mm`/`copies` ยังอยู่แต่ถูกเมิน. (A4 `document_settings.copies` ไม่เกี่ยว — ยังใช้อยู่)
- **พนักงานขาย (salesperson)** = field ใหม่บน `SaleForPrint.salesperson_name`. POS ดึงจาก `getCurrentUserName()` (userStore) ทั้ง snapshot + previewSale; reprint ดึงจาก `normalizeSale` (`detail.sold_by_name`, มาจาก `reports:getSaleByInvoice` join `u.name`).

**ไฟล์ที่แตะ:** `sections.ts` (ใหม่), `buildSlipHtml.ts` (rewrite section-driven), `schema.ts` (CREATE + migration ALTER/DROP), `types/index.ts` (ReceiptSettings + SaleForPrint), `ReceiptSettingsTab.tsx` (UI ราย section ในแท็บ "รูปแบบ" + DEFAULTS + SAMPLE_SALE), `userStore.ts` (`getCurrentUserName`), `normalizeSale.ts`, `POS/index.tsx`. save-handler `settings:saveReceiptSettings` เป็น dynamic `Object.keys()` UPDATE อยู่แล้ว — ทุกคีย์ในฟอร์มเป็นคอลัมน์จริง. tsc ผ่าน; **ยังไม่ได้ทดสอบในแอปจริง**.

**หัวกระดาษตามสถานะ VAT — DONE 2026-06-12 (tsc PASS; in-app click-test pending).** แผน = `docs/plans/Receipt_Title_By_Vat_Status.html`. ลำดับลอจิก: void→return→`(total_vat ?? 0) > 0`?abbrevTax:receipt ทั้งหมดรวมใน helper กลาง `resolveSlipMode(sale: SaleForPrint): SlipMode` ใน `src/lib/receipt/print.ts` แทนตรรกะซ้ำ 3 จุดเดิม: `POS/index.tsx` (printCompletedSale), `Manage/Sales.tsx` และ `components/dialogs/SaleDetailDialog.tsx` (reprintReceipt — ลบ getReceiptSettings()/abbrev ออก; printSlip โหลด config เองภายใน). **ผลลัพธ์:** หัวกระดาษขึ้นกับ `sale.total_vat` ของบิลล้วน ๆ; การควบคุมรายบิลยังอยู่ที่ checkbox VAT ใน payment dialog (ไม่ติ๊ก→total_vat=0→ใบเสร็จรับเงิน). **ReceiptSettingsTab.tsx:** ลบ Toggle "ใช้สลิปเป็นใบกำกับภาษีอย่างย่อ"; preview ใช้ `previewSale = vatEnabled ? SAMPLE_SALE : {...SAMPLE_SALE, total_vat:0}` แล้ว `resolveSlipMode(previewSale)` (อิง `useShopVat`). **⚠️ คอลัมน์ `abbrev_tax_invoice` (receipt_settings) → dead column ชั่วคราว คงไว้ใน DEFAULTS/type/schema (ไม่มี migration); จดไว้ลบจริงในรอบปรับ schema ก่อน launch** (รวมกับ dormant cols อื่น เช่น `paper_height_mm`/`copies`). ไฟล์ที่แตะ: `print.ts`, `POS/index.tsx`, `Manage/Sales.tsx`, `SaleDetailDialog.tsx`, `ReceiptSettingsTab.tsx` (5 ไฟล์; priest PASS ไม่มีบั๊ก; hunter tsc PASS — error เดียวที่เหลือ = Theme/index.tsx "warning-outline" ไม่เกี่ยวงานนี้). **⚠️ call-site ที่ 4 ที่แผนเดิมตก (แก้ตามหลัง 2026-06-12):** preview สลิปสดใน **payment dialog** (`POS/index.tsx` ~1705) เป็น **JSX literal ที่เขียนหัวกระดาษ hardcode "ใบเสร็จรับเงิน"** ไม่ได้ผ่าน `buildSlipHtml` → ไม่เปลี่ยนตาม checkbox VAT. แก้โดย export `TITLES` จาก `buildSlipHtml.ts` + เพิ่ม facade `slipTitle(sale)` ใน `print.ts` (= `TITLES[resolveSlipMode(sale)]`) แล้ว badge เรียก `{slipTitle(previewSale)}` (previewSale.total_vat ผูกกับ vatChecked→vatApplied→pendingVat อยู่แล้ว). บทเรียน: preview สดในแอปบางจุดวาดสลิปด้วย JSX มือ ไม่ใช่ builder — ต้องไล่หาแยกจาก call-site การพิมพ์.

**+ รวม preview เป็น component กลาง `SlipPreview` — DONE 2026-06-12 (tsc PASS, in-app verify pending).** เจ้าของขอให้ preview ใน **POS payment dialog** กับ preview ใน **Settings (ReceiptSettingsTab)** เป็น "ตัวเดียวกัน" (ดึง builder + data ทางเดียว เลิก drift) แต่ override ฟอนต์เป็น **ฟอนต์โปรแกรม** (ไม่ใช่ฟอนต์ใบเสร็จที่ตั้งใน Settings). ทำ: สร้าง `src/components/receipt/SlipPreview.tsx` (โฟลเดอร์ใหม่ `components/receipt/`) = iframe render ผ่าน `buildSlipHtml` + ขอบกระดาษฉีก (CSS mask, ย้ายมาจาก ReceiptSettingsTab) + auto-fit iframe height + zoom ผ่าน `transform:scale`. props: `sale/shop/settings/mode?/fontFamily?/zoom?`; **`fontFamily` override = แทน `settings.font_family` ตอน build** (ส่ง `{...settings, font_family: fontFamily}` เข้า buildSlipHtml ไม่ต้องแก้ signature). ใช้ 2 ที่: ReceiptSettingsTab (ไม่ส่ง fontFamily → ฟอนต์ใบเสร็จจริง + zoom control) และ POS payment dialog (`fontFamily="IBM Plex Sans Thai"` = ฟอนต์ไทยของแอป ซึ่งมีใน FONT_REGISTRY/print-safe; Inter ไม่ได้ bundle จึงไม่ใช้). **ลบ JSX literal slip เดิมใน POS ทิ้ง** (drift hazard) + **ลบ facade `slipTitle`/`TITLES` export ที่เพิ่งเพิ่ม** (ไม่ใช้แล้ว เพราะ preview ใช้ buildSlipHtml ตรง ๆ). **perf:** เพิ่ม cache ใน `buildPrintFontFaceCss` (`src/lib/print/fonts.ts`, Map ต่อ family) เพราะ payment dialog rebuild ถี่ทุก keystroke — TTF→base64 deterministic ต่อ family แคชได้ปลอดภัย. ไฟล์ที่แตะ: `components/receipt/SlipPreview.tsx` (ใหม่), `POS/index.tsx`, `ReceiptSettingsTab.tsx`, `print.ts`, `buildSlipHtml.ts`, `lib/print/fonts.ts`.
