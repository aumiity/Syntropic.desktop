# ✅ ระบบที่ทำเสร็จแล้ว

> กลับไป [[สรุป-00-สารบัญ]] · timeline ฟีเจอร์ที่ ship แล้ว (เรียงใหม่→เก่า)

---

## 🔐 ระบบ User / Login (2026-06-05 → 06)
- Login จริง: เลือก username + password (scrypt hash) + lockout (5 fails → 30s backoff)
- Session ฝั่ง main + บังคับ role ราย IPC (`requireAdmin`)
- Manager-override = inline credential (ไม่มี token รั่ว)
- Self-service recovery-code reset (ลืมรหัส → recovery code → รหัสใหม่)
- Setup Phase 0: ตั้งรหัสผ่านผู้ดูแล + แสดง recovery code ครั้งเดียว
- 2026-06-06: ขยาย profile (first/last/username unique/phone), staff เปลี่ยนรหัสตัวเองได้, เมนูบัญชีย้าย titlebar → sidebar (`SidebarUser.tsx`), e2e 29/29 PASS
- Role: admin / staff (ตัด pharmacist ออก)
- 🔗 memory: `project_user_login_licensing`, `ipc-role-enforcement`, `project_login_mockup`

## 💰 Expenses / Bookkeeping (2026-06-03)
- แท็บใหม่ `/reports/expenses` (ระหว่าง ซื้อ กับ รายงาน อย.)
- 2 ตาราง: `expense_categories` (seed 9 หมวด: ค่าเช่า/น้ำ/ไฟ/เงินเดือน/การตลาด/ขนส่ง/อุปกรณ์/ภาษี/อื่นๆ) + `expenses` (code `EX-YYYYMMDD-NNNN`)
- admin เพิ่ม/แก้/ลบ (hard delete) · staff อ่านอย่างเดียว + clamp 7 วัน
- Finance รวมเป็น 6 KPI: เพิ่ม ค่าใช้จ่าย + กำไรสุทธิ (= sales_profit − expense_total)
- 🔗 memory: `project_expenses`

## 💵 ระบบ Database Backup/Restore (2026-06-04)
- Backup `.db` เต็มไฟล์ + restore แบบ stage-then-relaunch (validate → snapshot → swap ตอน boot ถัดไป)
- Auto-backup = on-quit (VACUUM INTO, WAL-safe) + midnight timer
- เลือก `backup_dir` ได้ · ไฟล์ตั้งชื่อตามวันที่ · write-tmp-then-rename กัน 0-byte
- 🔗 memory: `project_db_backup`

## 🧾 ระบบเอกสารขาย (2026-05-31)
- **พิมพ์ใบเสร็จ/สลิปเงินสด** (80mm) + **ใบกำกับภาษีอย่างย่อ** (ม.86/6) + **เต็มรูป A4** (ม.86/4) + ตาราง `tax_invoices`
- **ใบเสนอราคา (Quotation)** — code `QT-YYYYMMDD-NNNN`, แก้/ลบเฉพาะ draft, พิมพ์ A4
- **แปลงใบเสนอราคา → การขาย** — state machine accepted→converting→converted, atomic claim กันขายซ้ำ, โหลดเข้า POS cart
- ⚠️ Quotation ตอนนี้ **ซ่อนจาก nav** แล้ว (ดู [[สรุป-04-งานที่ค้างอยู่]] FlowAccount)
- 🔗 memory: `project_sales_documents`, `project_receipt_sections`

## 🏪 First-run Setup Wizard (2026-05-31)
- Wizard บังคับตอนติดตั้ง: ข้อมูลร้าน (ชื่อ/ที่อยู่/เบอร์ required) + ตัดสินใจ VAT ครั้งเดียว + ตั้งรหัสแอดมิน
- Gate ใน `App.tsx` — POS เข้าไม่ได้จนกว่าจะ setup เสร็จ
- ⚠️ VAT-registered ถูก**บล็อก**ที่ setup (Phase 2/3 ยังไม่พร้อม) → ดู [[สรุป-04-งานที่ค้างอยู่]]
- 🔗 memory: `project_vat_phasing`

## 🛒 ระบบ POS + ราคา
- **VAT (output)** — VAT-inclusive, all-or-nothing, snapshot per-sale (2026-05-30)
- **POS qty multiplier** — พิมพ์ `5*` แล้วสแกน → ได้ 5 ชิ้น (number-first, `*` = commit)
- **POS cart alerts** — เตือนหมดอายุ + สต็อกต่ำ ราย row (`sales_settings` table)
- **Negative-stock sales + reconciliation** — ขายติดลบได้ + ตัดยอดย้อนหลังตอน GR เข้า

## 📦 ระบบ Product Bundle (ชุดสินค้า) (2026-05-20)
- `is_bundle` + `product_bundle_items`, FEFO deduct ราย component
- atomic create + invariant ≥2 components, return ชุด, audit 6 critical fixes
- bundle ห้ามถูก GR / ปรับ stock/lot โดยตรง (assertNotBundle)

## 📋 ระบบรายงาน อย.
- **ขย.9 (บัญชีการซื้อยา)** — A4 landscape WYSIWYG + print (2026-05-20)
- **ขย.10/ขย.11** — ⬜ DEFERRED (Phase 5, รอ spec อย. — อย่าเริ่มเชิงรุก)

## 🏷️ ระบบฉลากยา (2026-06-07)
- section SSOT (`sections.ts`), shared React preview, dual render (React + HTML-string)
- locked section order, per-product preview
- ⬜ ค้าง: per-product PRINTING ยังไม่ wired + section "ชื่อสามัญ" (รอ DB column)
- 🔗 memory: `project_drug_label`

## 📅 MultiDatePicker (2026-06-08)
- picker วันที่มาตรฐานใหม่ (day/month/year/custom + stepper + presets) แทนทั่วแอป (commit `65c9907`)
- 🔗 memory: `project_multi_date_picker`

## 🖨️ Printer settings hub (2026-06-02)
- แท็บ "เครื่องพิมพ์" รวม A4/ฉลากยา/ใบเสร็จ; A4 docs ใช้ printer เดียวผ่าน `document_settings`
- 🔗 memory: `project_printer_settings`

---

➡️ อะไรยังไม่เสร็จ: [[สรุป-04-งานที่ค้างอยู่]]
